-- Database schema for the Dunbar Vet appointment system.
--
-- Sprint 0 scaffold only. Domain tables (clients, animals, properties,
-- consultations, farm visits) are introduced by their respective feature
-- branches / sprint stories so schema changes are reviewed in pull requests.

PRAGMA foreign_keys = ON;
